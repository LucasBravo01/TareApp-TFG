<!DOCTYPE html>
<html lang="es">

<body>
    <nav class="d-flex align-items-center justify-content-between bg-ta-primary">
        <!-- Menú hamburguesa -->
        <button id="button-nav-hamburger" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample" aria-controls="offcanvasExample" aria-expanded="false" title="Desplegable del menú">
            <svg xmlns="http://www.w3.org/2000/svg" width="30" fill="white" class="bi bi-list" viewBox="0 0 16 16">
                <path fill-rule="evenodd" d="M2.5 12a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5m0-4a.5.5 0 0 1 .5-.5h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5" />
            </svg>
        </button>
        <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
            <div class="offcanvas-header bg-ta-primary">
              <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body bg-ta-primary">
                <ul>
                    <li><a href="/tareas/crearTarea">Crear Tarea</a></li>
                    <li>
                        <a href="/notificaciones" class="position-relative" >Notificaciones
                        <% if (generalInfo.notificationsUnread > 0 ) { %>
                            <span id="span-num-notifications" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-ta-blue" title="Correos no leídos">
                                <%= generalInfo.notificationsUnread %>
                            </span>
                        <% } %>
                        </a>
                    </li>
                </ul>
                <form id="form-logout" method="POST" action="/logout">
                    <a id="a-logout"><button class="m-0 p-0">Cerrar sesión</button></a>
                </form>
            </div>
        </div>
        <!-- Inicio -->
        <a id="a-nav" href="/" class="d-flex align-items-center" title="Volver a inicio">
            <img src="/images/logos/logoTareApp.png" alt="Logo TareApp" width="100">
        </a>
        <!-- Botón de perfil normal -->
        <a href="/perfil" title="Ir al perfil">
            <img id="img-nav-profile" src="/images/default/profile.png" alt="Foto de perfil" width="50">
        </a>
    </nav>
</body>

</html>